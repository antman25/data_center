node()
{
	stage('gitlab clone')
	{
		checkout scm
	}
	
        stage('Create Jobs')
        {
                 jobDsl targets: ['jenkins-library/dsl/jobs/build_root.groovy', 'jenkins-library/dsl/jobs/docker_ansible.groovy'].join('\n'),
                 removedJobAction: 'DELETE',
                 removedViewAction: 'DELETE',
                 lookupStrategy: 'SEED_JOB',
                 additionalParameters: [message: 'Hello from pipeline', credentials: 'SECRET']
        }
	stage('Build docker-ansible')
	{
		build job: '/build-root/docker-ansible/build'
	}
}
